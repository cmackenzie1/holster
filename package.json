{
  "name": "root",
  "private": true,
  "workspaces": [
    "workers/*"
  ],
  "scripts": {
    "lint": "prettier --check '**/*.{js,ts,css,json,md}'",
    "format": "prettier --write '**/*.{js,ts,css,json,md}'",
    "test": "echo 'No tests yet!'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.3",
    "wrangler": "^3.19.0"
  }
}
