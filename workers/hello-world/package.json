{
	"name": "hello-world",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"deploy:prod": "wrangler deploy --env production",
		"test": "vitest"
	},
	"dependencies": {
		"analytics": "file:../../lib/analytics"
	}
}
