{
  "name": "apigw",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "node build.mjs",
    "test": "npm run build && NODE_OPTIONS=--experimental-vm-modules jest --config jest.config.js --verbose"
  },
  "devDependencies": {},
  "dependencies": {}
}
