{
	"name": "gradient",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check-types": "tsc --noEmit",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"typegen": "wrangler types"
	},
	"dependencies": {
		"hono": "^4.11.9"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"@holster/typescript-config": "workspace:*",
		"typescript": "catalog:",
		"wrangler": "catalog:"
	}
}
